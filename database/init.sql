-- 테이블 생성 [SR-001]. battery / prediction / bms 테이블
-- battery 테이블에 init_capacity 컬럼 꼭 넣기(SOH→Capacity 역산용)

CREATE TABLE battery (
    -- TODO: init_capacity 등 컬럼 정의
);

CREATE TABLE prediction (
    -- TODO: 예측 관련 컬럼 정의
);

CREATE TABLE bms (
    -- TODO: BMS 관련 컬럼 정의
);