spring:
  datasource:
    url: jdbc:mysql://localhost:3306/kes?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: kes
    password: kes1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080